// test.syn

fnc printf(fmt: str, ...);

struct Entity {
    id: i32,
    health: f64,
    active: bln
}

fnc main() {
    boss: Entity = Entity { .id = 999, .health = 1000.5, .active = true };
    printf("Boss spawned with ID: %d\n", boss.id);
}
